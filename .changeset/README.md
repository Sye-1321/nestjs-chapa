# Changesets

Package-impacting pull requests add a Changeset with `pnpm changeset`. During M1 the package remains private: Changesets records version intent only, and no publish or release workflow exists.
