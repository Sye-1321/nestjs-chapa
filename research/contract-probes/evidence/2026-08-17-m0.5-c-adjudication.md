# M0.5-C Adjudication and Closeout

Date: 2026-08-17
Status: Closed

## 1. OQ-03: Identity-Field Requiredness
- **candidate-V**: In the exact observed Test Mode hosted-initialization path, `first_name`, `last_name`, and `email` were not required when `amount`, `currency`, and `tx_ref` were supplied.
- **U**: Whether all identity/customer fields are universally optional, and live-mode parity.

## 2. OQ-04: tx_ref Format/Length
- **candidate-V**: Test Mode maximum-length behavior of the exact tested cases (50-character `tx_ref` accepted, 51-character `tx_ref` rejected with message stating it must not exceed 50 characters).
- **U**: The complete allowed character grammar and behavior for all other references.

## 3. OQ-08: Duplicate tx_ref Behavior
- **U**: HTTP status, message, success/failure, or collision semantics. The duplicate POST occurred but local capture collided with a test artifact and the provider response was unavailable. No replay occurred.

## 4. OQ-09: Unpaid/Pending Verification

- **candidate-V**:
  - A freshly initialized, untouched Test Mode transaction was returned by verification with HTTP 200 and `data.status='pending'`.

- **candidate-V (sanitized observation; raw no longer retained)**:
  - The exact never-used provider-style reference `m05c_unknown_e0f64a2ac2618f7d` returned HTTP 400 with `"Invalid transaction reference"` in Test Mode.

- **U**: Universal unpaid => HTTP 404 behavior, exact stable unpaid discriminator, whether all untouched transactions behave identically, and live-mode parity. General behavior for all nonexistent references and any relationship between unknown-reference behavior and documented unpaid/404 behavior.

- **Note**: HTTP 200/top-level success must not be treated as payment success; transaction state came from `data.status` in this observation. Do not generalize the exact 400 result.

## 5. OQ-10: Amount Grammar
- **candidate-V**:
  - "10" (accepted)
  - "0" (rejected, validation.min.numeric)
  - "-1" (rejected, validation.min.numeric)
  - "1e2" (accepted)
  - "1,000" (rejected for this exact input, validation.numeric)
- **candidate-V (sanitized observation; raw no longer retained)**:
  - "1.12345678901234567890" (accepted)
  - "00010.50" (accepted)
  - "999999999.99" (rejected, amount must not exceed 1000000)
- **U**: COMPLETE provider amount grammar. Do not infer arbitrary exponent notation validity, arbitrary decimal scale validity, exact accepted maximum is exactly 1000000, exact minimum beyond observed cases, normalization/preservation behavior, and live-mode parity.
- **Classification Confidence**: Part 2 raw captures for C11/C12/C13/provider-style unknown were accidentally removed after sanitization; these observations are not equivalent to retained-raw evidence.

## 6. Initialization/Verification Lifecycle Facts
- **candidate-V**:
  - POST initialize with amount "10", ETB, accepted `tx_ref` and no email returned HTTP 200 with Hosted Link and `checkout_url`.
  - Verify of that exact untouched transaction returned HTTP 200 with `data.status` pending.
  - Provider wire verification represented amount as JSON number `10` despite initialization submitting string `"10"`.
- **Note**: Do NOT turn provider wire representation into a public SDK money-type decision yet.

## 7. Failed-State Probe
- **U / deferred**: No payment/checkout activity was used to manufacture a failed transaction. This was intentionally not forced.

## 8. Uncertain Initialization Procedure
- **Classification**: Harness/safety verification, NOT provider-contract evidence. (Proven locally using fake/injected failure tests: one attempt, preserve `tx_ref`, no POST replay, no automatic verify, separate future verification authorization required).

## 9. Evidence Integrity Note
- Part 1 evidence checkpoint retained as committed sanitized evidence.
- Part 2 sanitized evidence committed.
- Some Part 2 ignored raw captures were accidentally removed after review.
- Duplicate real response was never captured.
- No observation was reconstructed by replay.
- No live-mode requests.
- No payment attempts.
- No automatic retries.

## 10. Session Accounting
- total provider requests: 16
- POST initialize: 14
- GET verify: 2
- automatic retries: 0
- transport uncertainty: 0
- live requests: 0
- checkout/payment interactions: 0

## 11. Closeout Decision
- **Ready to close**: YES
- Remaining uncertainties are explicitly preserved as **U** and do not justify additional mutation/research requests in M0.5-C.
- Cancellation and refund work belongs to M0.5-D.
- Webhook work belongs to M0.5-E.
