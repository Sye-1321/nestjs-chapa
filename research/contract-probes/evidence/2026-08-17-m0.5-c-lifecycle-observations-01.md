# M0.5-C Payment Lifecycle — Sanitized Observations 01

Date: 2026-08-17
Environment: Chapa Test Mode
State: Candidate evidence pending maintainer adjudication

## Safety

- All provider requests individually human-authorized.
- Initialization: POST /v1/transaction/initialize.
- Verification: separately authorized GET.
- Timeout: 5000 ms.
- Exactly one attempt per invocation.
- Automatic retries: 0.
- Transport uncertainty: 0.
- Live-mode requests: 0.
- Raw captures remain git-ignored under `.raw/`.
- Checkout URL/token values are excluded.

## C2-INIT-BASELINE-01

Input:
- amount: `"10"`
- currency: `"ETB"`
- first_name: `"M05C"`
- last_name: `"Research"`
- email: `"m05c@example.com"`

Observed:
- HTTP 400
- top-level status: `failed`
- data: `null`
- `message.email = ["validation.email"]`

Candidate conclusion:
The exact supplied email value was rejected. This does not establish that
email is required and does not generalize to every address in that domain.

## C2-INIT-BASELINE-02

Input:
- amount: `"10"`
- currency: `"ETB"`
- first_name: `"M05C"`
- last_name: `"Research"`
- email omitted

Observed:
- HTTP 200
- status: `success`
- message: `Hosted Link`
- checkout URL present

Candidate conclusion:
This exact Test Mode payload was accepted without email.

## C3-VERIFY-CREATED-01

Observed:
- HTTP 200
- top-level status: `success`
- message: `Payment details fetched successfully`
- `data.status = "pending"`
- `data.mode = "test"`
- `data.method = null`
- `data.type = "Unknown"`
- currency: `ETB`
- amount runtime type: number
- amount value: `10`
- charge: `null`
- tx_ref matched expected value
- provider reference absent
- email: `null`
- phone_number: `null`

Candidate conclusion:
HTTP 200 and top-level `success` do not establish payment success.
Transaction state must be derived from `data.status`.

The provider returned amount as a JSON number even though initialization
supplied a decimal string. This is a provider-wire observation only.

## C4-INIT-MINIMAL-IDENTITY-01

Input contained only:
- amount: `"10"`
- currency: `"ETB"`
- tx_ref

Observed:
- HTTP 200
- status: `success`
- message: `Hosted Link`
- checkout URL present

Candidate conclusion:
For this observed Test Mode path, first_name, last_name, and email were not
required.

## C5-INIT-MISSING-AMOUNT-01

Observed:
- HTTP 400
- status: `failed`
- data: `null`
- `message.amount = ["validation.required"]`

Candidate conclusion:
Amount was required for this observed initialization path.

## C6-AMOUNT-ZERO-01

Input:
- amount: `"0"`

Observed:
- HTTP 400
- `message.amount = ["validation.min.numeric"]`

Candidate conclusion:
`"0"` reached the numeric minimum validator and was rejected.

## C7-AMOUNT-NEGATIVE-01

Input:
- amount: `"-1"`
- tx_ref length: 51

Observed:
- HTTP 400
- `message.amount = ["validation.min.numeric"]`
- `message.tx_ref = ["The tx ref must not exceed 50 characters."]`

This request contained two invalid variables and is not the isolated
negative-amount observation.

## C7B-AMOUNT-NEGATIVE-ISOLATED-01

Input:
- amount: `"-1"`
- tx_ref length: 25

Observed:
- HTTP 400
- data: `null`
- `message.amount = ["validation.min.numeric"]`
- no tx_ref validation error

Candidate conclusion:
`"-1"` independently reached the numeric minimum validator and was rejected.

## C8-TXREF-LENGTH-50-01

Input:
- amount: `"10"`
- currency: `"ETB"`
- tx_ref length: exactly 50

Observed:
- HTTP 200
- status: `success`
- message: `Hosted Link`
- checkout URL present

Boundary evidence:
- length 50: accepted
- length 51: rejected with explicit maximum-50 validation message

Candidate conclusion:
Observed Test Mode behavior supports a maximum tx_ref length of 50
characters. Live-mode parity remains unverified.

## Request Accounting

- Provider requests: 9
- POST initialization requests: 8
- GET verification requests: 1
- automatic retries: 0
- transport uncertainty: 0
- live-mode requests: 0
- payment attempts: 0
- checkout interactions: 0
- cancellations: 0
- refunds: 0
- webhook requests: 0

## Adjudication

These are sanitized candidate observations, not automatically promoted
verified contract claims.

Remaining M0.5-C work includes bounded amount grammar, duplicate
initialization behavior, provider-valid-style unknown verification,
naturally reproducible failed-state evidence if available, uncertainty-path
adjudication, and final evidence freeze.
