# M0.5-E Webhook Provider Observation

**State**: Sanitized Test Mode observation; pending E7 maintainer adjudication

This record contains no raw body, signature, digest, secret, URL, account identifier, transaction identifier, receipt identifier, or payment reference. The local raw capture remains ignored and untracked.

## Acquisition

- Environment: Chapa Test Mode
- Acquisition classification: one provider-generated webhook following the separately authorized Test Mode acquisition
- Sanitized acquisition ID: `M05E-E4-OBS-01`
- Intended captures completed: 1
- Raw body length: 459 bytes
- JSON parse after offline signature analysis: succeeded
- Additional provider requests during this evidence-recording task: 0
- Additional webhook acquisition during this task: 0
- Duplicate/redelivery count: not supplied in the authoritative observation; not inferred

## Signature observations

| Candidate | Header | Key candidate | Message candidate | Observed syntax | Match | Classification |
|---|---|---|---|---|---:|---|
| X1 | `x-chapa-signature` | `webhookConfiguredSecret` | exact raw webhook bytes | 64-character hexadecimal SHA-256 | true | candidate-V for this observation |
| X2 | `x-chapa-signature` | `apiSecretKey` | exact raw webhook bytes | 64-character hexadecimal SHA-256 | false | rejected for this observation |
| C1 | `chapa-signature` | `webhookConfiguredSecret` | UTF-8 bytes of `webhookConfiguredSecret` | 64-character hexadecimal SHA-256 | true | candidate-V for this observation |
| C2 | `chapa-signature` | `apiSecretKey` | UTF-8 bytes of `apiSecretKey` | 64-character hexadecimal SHA-256 | false | rejected for this observation |

Both headers were present. The two configured key values were deliberately distinct before acquisition, so this observation distinguishes X1 from X2 and C1 from C2. No conclusion is drawn about configurations not observed here.

## Raw-versus-reserialized diagnostic

- JSON parse succeeded: yes
- Exact raw length: 459 bytes
- Ordinary `JSON.stringify(JSON.parse(raw))` length: 459 bytes
- Exact raw bytes equal ordinary reserialized bytes: yes

This payload cannot distinguish signing exact raw bytes from signing this one byte-identical ordinary reserialization. It establishes no provider canonicalization rule and does not make reconstructed JSON safe. The normative verifier input remains the exact original raw body bytes.

## Sanitized event shape

Safe discriminants observed:

- `event`: `charge.success`
- `status`: `success`

Observed top-level fields and primitive categories:

| Field | Category |
|---|---|
| `amount` | string |
| `charge` | string |
| `created_at` | string |
| `currency` | string |
| `customization` | object |
| `email` | null |
| `event` | string |
| `failure_reason` | null |
| `first_name` | null |
| `last_name` | null |
| `meta` | null |
| `mobile` | null |
| `mode` | string |
| `payment_method` | string |
| `reference` | string |
| `status` | string |
| `tx_ref` | string |
| `type` | string |
| `updated_at` | string |

Observed nested `customization` fields:

| Field | Category |
|---|---|
| `customization.description` | null |
| `customization.logo` | null |
| `customization.title` | null |

Only field names and type categories are retained for sensitive or identifying fields. The `charge.success`/`success` structure is candidate-V only for this single Test Mode capture. Other events, statuses, optionality, value grammars, field stability, duplicate behavior, and Test Mode/live-mode parity remain D or U.

## Narrow conclusions and limitations

Candidate-V for this Test Mode observation:

- a provider webhook delivery was captured once;
- `x-chapa-signature` and `chapa-signature` were both present;
- both observed signatures had strict 64-character hexadecimal syntax;
- X1 reproduced `x-chapa-signature`;
- C1 independently reproduced `chapa-signature`;
- the sanitized `charge.success`/`success` field shape above was observed.

Rejected only for this observation:

- X2 did not reproduce `x-chapa-signature`;
- C2 did not reproduce `chapa-signature`.

Unresolved:

- live-mode parity and universal provider behavior;
- provider canonicalization or raw-versus-reserialized distinction;
- behavior if API and webhook secrets are configured differently in another account or mode;
- unobserved event/status variants and stable field optionality;
- any production acceptance rule involving both headers.
